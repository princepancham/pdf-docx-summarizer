# Phase 2 direct dependencies (backend + frontend + tests).
# requests is frontend-only (Streamlit -> FastAPI).
# pytest/httpx are test-only (TestClient-based upload tests).
fastapi
uvicorn
streamlit
python-dotenv
python-multipart
requests
pytest
httpx
