# Phase 2 + Document Processing/AI dependencies (backend + frontend + tests).
# requests is frontend-only (Streamlit -> FastAPI).
# pytest/httpx are test-only (TestClient-based tests).
fastapi
uvicorn
streamlit
python-dotenv
python-multipart
requests
pytest
httpx
pypdf
python-docx
